import * as React from 'react';
import Box from '@mui/material/Box';

export default function Config(playing: boolean) {
  return (
    <Box
      sx={{
        width: 200,
        height: 300
      }}
    >
        Config
    </Box>
  );
}